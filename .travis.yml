language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wpd' ]; then cd .. && eval "mv $currentfolder generator-wpd" && cd generator-wpd; fi

